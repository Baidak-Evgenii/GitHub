Задание 1

Предусловия:
а. Пользователь создан в github;
б. git установлен;
в. git config добавлен.

1. Создать свой репозиторий в таким же именем, как и имя пользователя:
// данное действие делал в GUI интерфейсе GitHub, вместе с созданием репозитория был создан файл README.md

2. Склонировать его на свой компьютер в отдельную папку:
// первой коммандой в консоле Bash, учитывая, что все действия для выполнения предусловия были выполненны верно, будет:

 $ git clone git@github.com:Baidak-Evgenii/Baidak-Evgenii.git

3. Склонировать себе следующий репозиторий в отдельную папку https://github.com/testrusau/testrusau:
// второй коммандой будет:

 $ git@github.com:testrusau/testrusau.git

4. Выберите один из любых вариантов и выполните его...
5. Вариант 1: Скопировать данные из склонированного репозитория и вставить их в ваш репозиторий из шага 2
// первой коммандой, для этого варианта будет:

  $ cd testrusau

// второй коммандой, будет:

  $ git push git@github.com:Baidak-Evgenii/testrusau.git git@github.com:Baidak-Evgenii/Baidak-Evgenii.git

// затем необходимо зайти в файл README.md и отредактировать его под себя (удобнее это делать через GUI Visual Studio)

// третьей коммандой, будет:
  
  $ git add .

// четвертой коммандой, будет:

  $ git commit -m "zapishi_vse_kak_est"  

// пятой коммандой, будет:

  $ git push 



